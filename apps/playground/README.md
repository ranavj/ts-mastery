# `apps/playground` — Week 12 Capstone

**Status:** 📋 Not Started

## Purpose

Ek chhota React app jisme dono libraries saath chalti hain — sirf yeh dikhane ke liye ki poora
system real app mein kaam karta hai. Yeh koi "product" nahi hai, sirf demo/proof-of-use.

- `packages/query` — kisi bhi public API se data fetch + cache (e.g. JSONPlaceholder)
- `packages/redux` — UI state (selected item, filter/sort)

## Design Questions

- Dono libraries ko ek saath use karte waqt koi overlap/conflict hota hai kya (dono `Provider`
  chahte hain — nesting order matter karta hai)?

## Notes

_(build karte waqt jo seekha, yahan)_
